{
  "providers": [
    "awscloudformation"
  ],
  "projectName": "ec2dashboard",
  "version": "3.1",
  "frontend": "javascript",
  "javascript": {
    "framework": "react",
    "config": {
      "SourceDir": "src",
      "DistributionDir": "build",
      "BuildCommand": "yarn run build",
      "StartCommand": "yarn run start"
    }
  }
}